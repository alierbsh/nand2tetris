.DS_Store

# Simulator state files — each holds only the last directory opened,
# so they change on every run and leak local filesystem paths.
nand2tetris/tools/bin/*.dat

# Test run outputs, regenerated by the simulators on every run.
# The expected values live in the tracked .cmp files.
*.out
